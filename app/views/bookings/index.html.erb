<div class="container">
  <div class="row">
    <h1>My bookings</h1>
  </div>

</div>
<ul>
<% @bookings.each do |booking| %>
<li> <%= link_to booking.description, booking_path(booking) %> <%= booking.booking_statuses.last.status %> </li>
<% end %>
</ul>

