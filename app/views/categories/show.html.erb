<div class="text-center padded features">
  <div class="banner search-show" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= image_path @category.photo %>');">
    <div class="banner-content">
      <div class="container">
        <h1><%= @category.name %></h1>
          <p>Choose from our catalogue of services now!</p>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
      <% @services.each do |service| %>
      <% if service.category_id == Category.find(params[:id]).id %>
    <div class="col-xs-12 col-sm-6 col-md-4">
          <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path service.photo %>');">
            <div class="card-category"><%= service.name %></div>
            <div class="card-description">
              <p> <%= service.hourly_rate %>€/h</p>
            </div>
            <%= cl_image_tag service.user.profile.photo , class: "card-user" %>

<!--             <img class="card-user" src="https://kitt.lewagon.com/placeholder/users/tgenaitay"> -->
            <%= link_to "", service_path(service), class: "card-link" %>
          </div>


    </div>
    <% end %>

    <% end %>

  </div>
<%= link_to "Back to categories", categories_path, class: 'btn btn-back-to-categories' %>
</div>

